name: Update Data and Deploy

on:
  schedule:
    - cron: '0 13 * * *' # Runs at 13:00 UTC (9 AM EST) daily
  workflow_dispatch:      # Allows manual "Run Now" button

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      # 1. Get your code
      - name: Checkout
        uses: actions/checkout@v3

      # 2. Set up Python and Run your Script
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          
      - name: Install Python Dependencies
        run: pip install pandas

      - name: Fetch FRED Data
        run: python scripts/fetch_data.py

      # 3. Commit the new data.json (So you have a history)
      - name: Commit Data Update
        run: |
          git config --global user.name 'LiquidityBot'
          git config --global user.email 'bot@noreply.github.com'
          git add public/data.json
          git commit -m "Auto-update liquidity data" || exit 0
          git push

      # 4. Build the React Website
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install NPM Packages
        run: npm install

      - name: Build Project
        run: npm run build

      # 5. Deploy to GitHub Pages
      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2